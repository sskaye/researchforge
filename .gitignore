# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Python (in case skills end up with Python)
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# Node (in case skills end up with Node)
node_modules/

# OS / misc
Thumbs.db

# Paper corpora — copyrighted journal PDFs/NXML/HTML, must not be committed to a public repo
# (see data_extraction_dev/README.md for rationale)
data_extraction_dev/corpora/
test_data/

# Copyrighted journal full-text bundled inside trial runs — same rationale as corpora/
data_extraction_dev/trials/**/*.pdf
data_extraction_dev/trials/**/*.nxml
data_extraction_dev/trials/**/*.html
# Bundled input paper corpora inside trial runs (article full-text, not trial results)
data_extraction_dev/trials/trial3/Trial3-full-gpt55_high/paper_root_index/
data_extraction_dev/trials/trial6/book/
data_extraction_dev/trials/trial8/opus47_books/books/
data_extraction_dev/trials/trial8/opus47_papers/Papers/

# Files staged for deletion during reorgs
_to_delete/
**/_to_delete/

# Per-paper symlink indexes pointing at the (gitignored) corpora; dangling outside dev box
**/phase3_paper_root_index/
